name: create_month_pages

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 1 * *"

jobs:

  create_notion_morning_pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Go
        uses: actions/setup-go@v2
        with:
          go-version: "1.17.0"
      - name: Create daily pages
        run: go run cmd/monthly_stats/main.go
        env:
          MORNING_WORKFLOW_API_TOKEN: ${{ secrets.MORNING_WORKFLOW_API_TOKEN }}
